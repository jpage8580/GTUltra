# Golden-reference corpus for the SID engine migration (M0 capture -> M1 bit-identical gate).
#
# Format (pipe-delimited, '#' comments and blank lines ignored):
#   name | song.sng | extra sng2wav args
#
# - name: stable identifier used as the manifest key.
# - song.sng: path relative to the repo root.
# - args: passed verbatim to sng2wav. ALWAYS pin length (--frames) + --seed for
#   reproducibility, and set --backend explicitly so both legacy engines are covered.
#
# Rendered WAVs are transient (never committed; see .gitignore). The reference is the
# sha256 manifest produced by `tests/integration/golden.sh capture`, which is
# per-build/per-arch (reSID float output differs across compilers/arch) and therefore
# NOT committed and NOT a cross-platform CI gate -- it is the M1 same-environment
# bit-identical check (capture before the refactor, check after, on one build).
#
# Maintainer: extend this list with the chosen corpus (6581/8580 x sync/ring-mod/
# filter-sweep x mono/SID-stereo/TrueStereo). Shortlist: notes/new-emu/golden-corpus-candidates.md

pendejo_residfp | tests/fixtures/Stereo_Pendejo.sng | --frames 1000 --backend legacy-residfp --seed 1
pendejo_resid   | tests/fixtures/Stereo_Pendejo.sng | --frames 1000 --backend legacy-resid   --seed 1
